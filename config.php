<?php
/* ─────────────────────────────────────────────────────────────
   약재검색포털 — 설정
   ⚠ 이 파일에는 접속 정보가 들어갑니다. .htaccess 로 직접 접근을 막고 있지만
     외부에 공유하거나 저장소에 올리지 마세요.
   ───────────────────────────────────────────────────────────── */

/* ── DB 접속 (카페24 나의서비스관리 > MySQL 정보) ──
   카페24 웹호스팅은 보통 아래가 모두 같은 값입니다.
     호스트 : localhost
     DB명   : 카페24 아이디
     사용자 : 카페24 아이디
     비밀번호: MySQL 비밀번호(호스팅 비밀번호와 다를 수 있음) */
const DB_HOST = 'localhost';
const DB_NAME = '카페24아이디';
const DB_USER = '카페24아이디';
const DB_PASS = 'MySQL비밀번호';

/* ── 화면 접속 비밀번호 ──
   평문을 적지 마세요. 아래 해시를 교체합니다.

   해시 만드는 법 — SSH 접속 후 한 줄 실행:
     php -r "echo password_hash('원하는비밀번호', PASSWORD_DEFAULT), PHP_EOL;"

   출력된 $2y$... 문자열을 통째로 아래에 붙여넣으세요.
   반드시 작은따옴표를 쓰세요. 큰따옴표를 쓰면 $ 가 변수로 해석돼 로그인이 실패합니다.

   아래는 교체 전 자리표시자입니다. 그대로 두면 로그인 화면에 설정 안내가 뜹니다. */
const APP_PASSWORD_HASH = 'PUT_YOUR_PASSWORD_HASH_HERE';

/* ── 세션 유지 시간 (초). 기본 12시간 ── */
const SESSION_LIFETIME = 43200;
